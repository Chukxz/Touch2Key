[system] # Auto generated, edit at your own risk.
hud_image_path = "./mp_hud.jpg" # Path to the HUD image file.
json_dev_dpi = 320 # Device DPI for the JSON file.
json_dev_name = "Chuksxz" # Device name for the JSON file.
json_dev_res = [720, 1612] # Device resolution for the JSON file.  
json_path = "" # Path to the JSON file defining mapping zones.

[key] # Auto generated, edit at your own risk.
mouse_wheel_conf = ['', 0.0, 0.0, 0.0] # Mouse Wheel Configuration: [scancode, center_x, center_y, radius].

[mouse] # Auto generated, edit at your own risk.
invert_y = false 
sensitivity = 15.0 # Adjust this multiplier to tune how fast the camera turns, roughly: 1.0 means "1 DP unit = 1 Mouse Mickey.

[joystick] # Auto generated, edit at your own risk.
deadzone = 0.15 # Deadzone (0.0 - 1.0).
fixed_center = false 
hysteresis = 5.0 # Hysteresis Angle (Degrees).
